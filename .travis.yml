sudo: required
dist: trusty
language: cpp
before_install:
  - sudo apt-get -qq update
  - >
    sudo apt-get install -y
    libarmadillo-dev
    libann-dev
    libjsoncpp-dev
    libasio-dev
script:
  - pushd ema-tracker
  - cmake cmake
  - make
  - popd
