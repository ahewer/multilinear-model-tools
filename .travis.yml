language: cpp
matrix:
  include:
    - os: linux
      sudo: required
      dist: trusty
    - os: osx
addons:
  apt:
    packages:
      - libarmadillo-dev
      - libann-dev
      - libjsoncpp-dev
      - libasio-dev
      - libyaml-cpp-dev
      - libinsighttoolkit4-dev
script:
  - pushd ema-tracker
  - cmake cmake
  - make
  - popd
